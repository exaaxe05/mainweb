service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // อนุญาตให้อ่านได้สาธารณะ (ถ้าต้องการแสดงรูปบนเว็บ) 
      // แต่เขียนต้องล็อกอิน หรือใช้สิทธิ์ Admin
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}