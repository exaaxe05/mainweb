service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // อนุญาตให้ทุกคนอ่านได้ แต่เขียนได้เฉพาะคนที่ผ่านการล็อกอิน (ถ้ามี)
      allow read: if true;
      allow write: if request.auth != null; 
    }
  }
}