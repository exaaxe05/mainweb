{
  "rules": {
    ".read": true,
    ".write": "auth != null || now < 1742054400000" 
    // เปลี่ยนตัวเลข 'now' เป็นเวลาในอนาคต (เช่น 1 เดือนจากนี้) 
    // เพื่อให้ ESP32 เขียนข้อมูลได้โดยไม่ต้องล็อกอินในช่วงที่กำลังทำ
  }
}